<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eatup">
s
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/eat_up"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EatUp">
        <activity android:name=".SelectedItems"></activity>
        <activity android:name=".extra_for_example" />
        <activity android:name=".ItemsOrders" />
        <activity android:name=".loginpage" />
        <activity android:name=".newuser" />
        <activity android:name=".PreviousOrders" />
        <activity android:name=".faq" />
        <activity android:name=".ProfileUser" />
        <activity android:name=".RestaurantNames" />
        <activity android:name=".HomeActivity" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>